# -----------------------------------------------
# !!! THIS FILE WAS GENERATED BY TYPE-GRAPHQL !!!
# !!!   DO NOT MODIFY THIS FILE BY YOURSELF   !!!
# -----------------------------------------------

input CreateNoteDto {
  content: String!
}

input CreateUserDto {
  avatar: String
  password: String!
  username: String!
}

"""
The javascript `Date` as string. Type represents date and time as the ISO Date string.
"""
scalar DateTime

input LoginUserDto {
  password: String!
  username: String!
}

type Mutation {
  createNote(noteData: CreateNoteDto!): Note!
  createUser(userData: CreateUserDto!): User!
  deleteNote(noteId: String!): Boolean!
  deleteUser(userId: String!): Boolean!

  """User login"""
  login(userData: LoginUserDto!): String!

  """User logout"""
  logout: User!

  """User signup"""
  signUp(userData: CreateUserDto!): String!

  """Favorite or un-favorite note"""
  toggleFavorite(noteId: String!): Note!
  updateNote(noteData: UpdateNoteDto!, noteId: String!): Note!
  updateUser(userData: UpdateUserDto!, userId: String!): User!
}

type Note {
  _id: ID!
  author: User!
  content: String!
  createdAt: DateTime
  favoriteCount: Int!
  favoritedBy: [User!]!
  updatedAt: DateTime
}

type NoteFeed {
  cursor: String
  hasNextPage: Boolean!
  notes: [Note!]!
}

input NoteFeedDto {
  cursor: String
  limit: Float
}

type Query {
  currentUser: User!
  getNoteById(noteId: String!): Note
  getNotes: [Note!]!
  getUser(username: String!): User!
  getUserById(userId: String!): User
  getUsers: [User!]!
  noteFeed(feedData: NoteFeedDto): NoteFeed!
}

input UpdateNoteDto {
  content: String!
}

input UpdateUserDto {
  avatar: String
  username: String!
}

type User {
  _id: ID!
  avatar: String
  createdAt: DateTime
  favorites: [Note!]!
  notes: [Note!]!
  roles: [String!]!
  updatedAt: DateTime
  username: String!
}
